import random                              # 난수를 사용할 수 있는 모듈을 불러온다

num = random.randint(1, 100)               # random 모듈의 randint 함수를 사용, random + integer 1이상 100 이하의 정수난수를 생성 (시작값, 종료값)

count = 0                                  # 입력횟수를 세기 위한 count 변수를 초기화
while True:                                # while 은 '~할 동안' 즉 조건반복. True는 참 그 자체이므로 while True 는 무한반복을 뜻함.
    count = count + 1                      # count 1 증가한다. 바로 맞춰도 한번만에 맞춘 것이므로 입력과 증가는 같이 실행된다.
    n = int(input("숫자를 입력하세요"))        # input() 속안에 넣은 문자열을 출력하고 값을 입력받는다. 입력값은 문자열이므로 int() 로 숫자로 만들어 변수 n 에 저장
    if n < num:                            # 만약 미리 정한 정답 num 이 입력한 n 보다 크다면, 
        print("더 큽니다")                   # '크다' 즉 더 큰 숫자 중에서 정답을 찾아야 한다는 것을 알려준다.
    elif n > num:                          # 다른 언어의 else if (아니고 만약) 를 파이썬에서는 줄여서 elif 로 사용한다.
        print("더 작습니다")                 
    else:                                  # 첫번째, 두번째 조건 모두 아닐 경우 - 정수 2개 a, b 의 관계는 'a가 크거나', 'b가 크거나', '서로 같거나' 의 3가지 경우가 있다.
        break                              # 맞췄을 경우 break 로 즉시 반복문을 종료한다. while 과 for 는 break 를 사용할 수 있다.
print(count, "번 만에 맞췄습니다")             # 문제를 맞출 경우에만 반복문이 종료되므로 이제 4칸 들여쓰지 않고 제일 앞칸에 작성한다.몇번만에 맞췄는지 알려준다.
