# 구구단 출력

for dan in range(2, 10):                   # dan 변수는 2부터 9까지 증가한다.
    for i in range(1, 10):                 # 각 단마다 1부터 9까지 곱할 숫자가 필요하다.
        print(dan, "*", i, "=", dan * i)   # 단 * 곱할 숫자 = 곱한 결과 를 출력한다.
    print()                                # 각 단이 끝나고 나면, 빈 줄을 출력해 보기 편하게 할 수 있다.
